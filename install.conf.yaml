- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.macos:
    ~/.vim:
    ~/.vimrc:

- shell:
  - git submodule sync --recursive